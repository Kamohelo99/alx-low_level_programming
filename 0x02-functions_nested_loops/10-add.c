/**
* add - adds two integers and returns the result
* @num1: the first integer for the calculation
* @num2: the second integer for the calculation
*
* Return: the sum of @n1 and @n2
*/
int add(int num1, int num2)
{
	return (num1 + num2);
}
